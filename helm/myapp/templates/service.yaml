apiVersion: v1
kind: Service
metadata: { name: myapp }
spec:
  selector: { app: myapp }
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      name: http
  type: {{ .Values.service.type }}
